#compile flags we will be using.
CFLAGS=-Wall -g
#our program name
PROGRAM=ex24
#the libs we want to use and be compiled too.
LIBS=ex22.o object.o

all: $(PROGRAM)

#ex22_main:$(LIBS)

clean:
	#rm *.o
	rm $(PROGRAM)
